def login():
    # Tạo cửa sổ mới
    new_window = tk.Tk()

    # Thêm nội dung vào cửa sổ mới
    new_window.title("Giao diện mới")
    new_window.geometry("400x400")

    # Hiển thị cửa sổ mới
    new_window.mainloop()